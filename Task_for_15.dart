// For 15. Дано вещественное число A и целое число N (> 0). Найти A в степени N :
// AN=A⋅A⋅...⋅A

// (числа A перемножаются N раз).
import 'dart:math';

void main() {
   
  double A = 1.5;
  int N = 3;
  
  for(int i = 0; i < N; i++) {
    
  }
  
  dynamic sum = pow(A,N);
  print(sum);
}
